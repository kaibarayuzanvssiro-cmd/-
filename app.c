#include <stdio.h>

int main(void) {
    /* 2つの整数を用意する */
    int a = 2;
    int b = 3;

    /* 足し算して結果をsumに入れる */
    int sum = a + b;

    /* 結果を画面に表示する */
    printf("%d + %d = %d\n", a, b, sum);

    return 0;
}
